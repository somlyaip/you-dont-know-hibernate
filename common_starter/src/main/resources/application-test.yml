spring:
  # Configure Spring Boot's Docker Compose integration for tests
  docker:
    compose:
      # Try project root first, then module root; whichever exists will be used
      file:
        - ../docker-compose.yml
        - docker-compose.yml
      # Ensure the database defined in compose is started for tests
      skip.in-tests: false
  jpa:
    properties:
      hibernate:
        use_sql_comments: true
        generate_statistics: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.stat: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE # for SpringBoot 3.*
#    org.hibernate.type.descriptor.sql.BasicBinder: TRACE # for SpringBoot 2.*
